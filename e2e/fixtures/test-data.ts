/**
 * Shared test data and credentials for E2E tests.
 *
 * These match the seed data created by `npm run db:seed`.
 */
export const ADMIN_USER = {
  username: "admin",
  password: "Admin@12345",
} as const;
